model MasterCountry {
  id         String           @unique
  name       String
  created_at DateTime         @default(now())
  updated_at DateTime         @updatedAt
  provinces  MasterProvince[]

  @@map("master_country")
}
