import catchErrors from "@helpers/catch-error";
import { TRequestAuthRoute } from "@types";
import { Response } from "express";

export const getCategorySkill= catchErrors(async(req:TRequestAuthRoute,res:Response)=>{
    
})