model Project {
    id                          String      @id @default(uuid())
    title                       String      @db.VarChar(255)
    thumbnail                   String      @db.VarChar(2083)
    description                 String      @db.LongText
    category                    CategoryProject
    type                        TypeProject

    menu_projects               MenuProject[]  
    responsibilities_project    ResponsibilityProject[]  
    tech_stacks                 SkillProject[]

    id_experiance               String      @db.VarChar(36)
    experiance                  Experiance  @relation(fields: [id_experiance], references: [id])

    id_user                     String      @db.VarChar(36)
    user                        User        @relation(fields: [id_user], references: [id])

    id_company                  String      @db.VarChar(36)
    company                     Company     @relation(fields: [id_company], references: [id])
    
    created_at                  DateTime    @default(now())
    updated_at                  DateTime    @updatedAt

    @@map("project")
}


enum CategoryProject{
    WEBSITE
    MOBILE
    API
    UI_UX
}


enum TypeProject {
    PERSONAL_PROJECT
    COMPANY_PROJECT
    FREELANCE
}